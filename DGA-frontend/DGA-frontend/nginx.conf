server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        # ถ้าหาไฟล์ไม่เจอ ให้ส่ง index.html กลับไป
        try_files $uri $uri/ /index.html;
    }
}